rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default rule for development: Allow read/write access to all documents
    // for any user that is signed in.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
